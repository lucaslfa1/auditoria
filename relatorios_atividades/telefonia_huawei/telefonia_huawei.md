# Relatório Técnico: Integração Telefonia Huawei e Resolução de Anomalias`n**Data:** 27 de Abril de 2026`n**Projeto:** Auditoria`n**Diretório Base:** C:\Users\lucas.afonso\projetos\auditoria`n`n---`n`n## 1. Contexto e Objetivos`nO objetivo desta frente de trabalho foi estabilizar, diagnosticar e corrigir as integrações de rede com a API da Huawei (AICC). A prioridade era resolver o bloqueio de rede (Erro HTTP 403) reportado pelo serviço do Google Cloud Run e corrigir a anomalia de paginação que ocultava as gravações sincronizadas da interface web de telefonia.`n`n---`n`n## 2. Atividade 1: Investigação e Resolução do Erro 403 (Autenticação Huawei)`n`n### 2.1. Diagnóstico Inicial`n- **Sintoma:** O arquivo de logs full_logs.json e as execuções do Cron registravam erros 403 Forbidden oriundos da comunicação com a Huawei.`n- **Topologia de Rede:** O sistema estava operando sob HUAWEI_AUTH_MODE=proxy, onde o tráfego era roteado e assinado via um endpoint intermediário na Teledata (c2Authorization.php).`n- **Ação Desejada:** O provedor da Huawei reportou a inclusão do IP estático do nosso Google Cloud NAT (35.199.111.152) em sua whitelist para acesso direto (usando fluxo OAuth 2.0 e o endpoint tokenByAkSk). O objetivo era testar a viabilidade do acesso direto sem quebrar a integração de produção atual.`n`n### 2.2. Implementação do Teste de Prova de Conceito (PoC)`n- Desenvolvemos um script isolado backend/tmp_test_huawei_oauth.py implementando estritamente a mesma cadeia HTTP da coleção do Postman (Huawei AICC BrazilSaaS-OPENTECH Lucas.postman_collection.json).`n- Fizemos o deploy do script na nuvem via **Cloud Run Job** (test-huawei-oauth), com o parâmetro --vpc-egress=all-traffic, garantindo que o teste utilizaríamos obrigatoriamente o IP 35.199.111.152 para atingir o WAF da Huawei.`n`n### 2.3. Execuções e Resultados (Logs)`n- **Tentativa 1 (Falha 403):** Ao invocar a API com as variáveis de ambiente atuais do sistema (HUAWEI_AK: 97501...), recebemos novamente o bloqueio.`n  - *Log Obtido:* Status Auth: 403 - Falha na Autenticação.`n  - *Hipótese Inicial:* Bloqueio de WAF por falta de propagação da whitelist do IP.`n- **Ação Corretiva (Engenharia):** Em testes paralelos e validação da coleção do Postman, o desenvolvedor (Lucas) notou que o app_key necessário para o endpoint de autenticação direta era estruturalmente diferente (formato UUID) do app_key esperado pelo proxy da Teledata.`n- **Tentativa 2 (Sucesso 200):** Alteramos o script para fazer *hardcode* das chaves diretas do Postman (app_key: <HUAWEI_DIRECT_APP_KEY>...).`n  - *Log Obtido:* Status Auth: 200 - Token recebido com sucesso!`n  - *Veredito:* A rede e o IP 35.199.111.152 **estão confirmadamente liberados** no ambiente da Huawei. O falso-positivo de bloqueio ocorria porque enviávamos credenciais desconhecidas (da Teledata) para a porta principal da Huawei, resultando em rejeição pelo Application Gateway.`n`n### 2.4. Próximos Passos Obrigatórios (Implementação Futura)`n- **Variáveis de Ambiente:** Adicionar novas credenciais exclusivas no .env do Cloud Run: HUAWEI_DIRECT_APP_KEY e HUAWEI_DIRECT_APP_SECRET.`n- **Refatoração no Backend:** O arquivo backend/core/huawei_client.py deve ser modificado para suportar o modo oauth_direct, onde o AccessToken retornado de tokenByAkSk é utilizado no Header Authorization: Bearer <token>. Isso permitirá **desativar definitivamente** a dependência da URL de proxy da Teledata.`n`n---`n`n## 3. Atividade 2: Resolução da Anomalia de Paginação (Fila de Triagem)`n`n### 3.1. Diagnóstico do Problema Visual`n- **Sintoma Relatado:** As ligações telefônicas eram reportadas como baixadas com sucesso e enfileiradas nos logs da execução (_enfileirar_classificado), mas a tabela na interface do Cloud Run (/api/telefonia/recordings) era carregada **vazia**.`n`n### 3.2. Causa Raiz Técnica (Anti-padrão de Filtragem)`n- A rota no backend (routers/telefonia.py) solicitava os dados executando database.listar_fila_revisao_classificacao(limit=50).`n- O PostgreSQL resolvia essa requisição trazendo as últimas 50 ligações enfileiradas **de qualquer origem** (o que incluía massa de dados gerada por uploads manuais em PDF/Excel).`n- Em seguida, o Python rodava uma List Comprehension para filtrar apenas aquelas originadas pela telefonia: if origem == 'huawei_sync'.`n- **O Bug:** Como os 50 registros recortados do banco não possuíam gravações da Huawei (pois estas estavam mais para trás na fila, nas posições 51+), o Python renderizava uma lista vazia, simulando falsamente a ausência dos áudios no sistema.`n`n### 3.3. Solução Implementada`nA lógica de filtragem foi movida inteiramente da aplicação (memória) para a camada de Banco de Dados (PostgreSQL):`n1. **Camada de Repositório (repositories/classification_review.py):**`n   - Injetamos o parâmetro opcional origem.`n   - Adicionada condição nativa de consulta em campo JSONB: filtros.append(`\"metadata_json::jsonb ->> 'origem' = %s`\").`n2. **Camada de Rotas (routers/telefonia.py):**`n   - A List Comprehension ineficiente foi removida.`n   - O argumento origem='huawei_sync' passou a ser repassado ao DAO, forçando o banco de dados a descartar as ligações manuais *antes* de aplicar o LIMIT 50.`n`n### 3.4. Testes, Tratamento de Conflitos e Lançamento`n- **Testes de Contrato:** Um erro transitório de import (REVIEW_QUEUE_STATUS_PENDING) em database.py foi exposto pelos testes unitários e prontamente corrigido. Todos os 12 testes do Módulo de Fila finalizaram em PASSED (pytest backend/tests/test_review_queue_contract.py).`n- **Resolução Git:** Um Merge Conflict severo (HEAD vs Origin) ocorreu em routers/telefonia.py durante o git pull --rebase. O conflito foi resolvido cirurgicamente priorizando a nova injeção JSONB, sem desestruturar as funções auxiliares _recording_item_from_queue recém trazidas da nuvem.`n- **Deploy e Sucesso:**`n  - Código commitado e mesclado: 7f32301 fix(telefonia): corrige filtro de origens no banco de dados para evitar paginacao vazia na fila de revisao.`n  - Lançamento no Cloud Run efetuado via gcloud run deploy auditoria --source ..`n  - A revisão ativa provê agora exibição fluida e contínua das auditorias importadas via Huawei, ignorando poluição de dados manuais na fila.`n`n---`n`n## 4. Orientações Gerais de Desenvolvimento`n1. **Autenticação:** Não submeter payloads com credenciais da Teledata (HUAWEI_AK: 975...) diretamente à brazilsaas.aicccloud.com. Use apenas o UUID (a536a...) associado ao endpoint de governança (oauth/tokenByAkSk).`n2. **Manipulação de Filas (Performance):** É expressamente proibido usar List Comprehensions do Python para filtrar dados da Fila de Revisão que já foram paginados/limitados. Todo filtro (status, setor, origem e dados dentro de metadata_json) deve ser injetado nas listas filtros e params dentro do repositório correspondente no Postgres para evitar falhas silenciosas de dados desaparecendo da UI.`n3. **Ambiente de Teste:** Caso seja necessário rodar diagnósticos restritos de firewall contra parceiros (ex: Huawei), instancie sempre um Job no Cloud Run amarrado à auditoria-vpc-connector para garantir saída com IP Estático mapeado (35.199.111.152). Testes locais resultarão em 403 Forbidden.
