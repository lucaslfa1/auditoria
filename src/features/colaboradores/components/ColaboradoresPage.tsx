/**
 * Re-export: a tela de Colaboradores É a gestão de operadores que vive em
 * `settings/components/OperadorManagement` (ColaboradorManagement). Mantido como
 * ponto de entrada da feature `colaboradores`; a lógica e os endpoints estão lá.
 */
export { ColaboradorManagement as ColaboradoresPage } from '../../settings/components/OperadorManagement';
