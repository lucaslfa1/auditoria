# v1.3.123 — Adapter azure_blob dormente no media storage (preparação, não migração)

**Data:** 2026-06-11
**Tipo:** Handover — portabilidade de storage (código dormente)
**Custo:** nenhum (não ativa sem env; dependência comentada no requirements)
**Branch:** `handover`

## Contexto

`core/media_storage.py` suportava `local` e `gcs` (produção atual no Cloud
Run). Para o handover, o time de engenharia ganha a OPÇÃO de Azure Blob
Storage sem precisar escrever o adapter. **Escopo confirmado pelo Lucas:
apenas preparar — nenhum serviço Azure é criado por este lado, e o destino
de storage (como o de banco) ainda não está definido.**

## Mudanças

### `backend/core/media_storage.py`
- Novo backend **`azure_blob`** espelhando os 4 caminhos do GCS
  (store/load/delete/stream), via `MEDIA_STORAGE_BACKEND=azure_blob` +
  `AZURE_STORAGE_CONNECTION_STRING` (+ `AZURE_STORAGE_CONTAINER`, default
  `auditoria-midia`).
- Import **lazy** do SDK (`azure-storage-blob`): a imagem atual (GCP) não
  precisa do pacote; erro claro se a env obrigatória faltar.
- Docstring do módulo documenta os 3 backends e a seleção.

### `backend/requirements.txt`
Linha COMENTADA `# azure-storage-blob>=12.19` — descomentar só se o time
optar por Blob. Sem mudança na imagem atual.

### `tests/backend/test_media_storage_azure_blob.py` (novo)
4 testes com SDK FAKE injetado em sys.modules (não exige o pacote):
roundtrip store/load (content-type e overwrite), stream em chunks com
tamanho, delete físico + registro, e erro claro sem connection string.

## Validação
- ✅ 4/4 testes novos verdes; `compileall` limpo.
- ✅ Nenhuma mudança nos caminhos `local`/`gcs` (produção atual intocada).

## Deploy
Nenhum necessário (código dormente). Se o time optar por Blob: descomentar a
dependência, buildar imagem, setar as 2-3 envs.
