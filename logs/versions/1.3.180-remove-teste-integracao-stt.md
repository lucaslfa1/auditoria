# v1.3.180 — Remove teste de integração STT manual + arquiva fixture de 13.7 MB

**Data:** 2026-06-16
**Tipo:** Limpeza de repositório / testes (chore)
**Custo:** nenhum
**Branch:** `main`

## O que foi feito

Removidos:
- `tests/backend/test_speech_stt.py` — teste de **integração manual**: só roda com
  `RUN_SPEECH_STT_INTEGRATION` ligada, faz uma chamada **paga ao vivo** ao Azure STT
  (`services.transcribe_audio`) e nunca participa da suíte automatizada (skip por
  default). O pipeline de STT já é coberto por testes mockados.
- `tests/backend/fixtures/20260217114030490_Fabiula_de_Espindola_BAS_Voz.wav`
  (13.7 MB) — fixture usada **apenas** por esse teste; era o maior arquivo do repo.
  Arquivada em `../auditoria-arquivo/` (fora do repo) para consulta, recuperável
  também pelo histórico git.

## Validação

- Era o único consumidor da fixture (grep confirmou uso só em `test_speech_stt.py`).
- Suite backend completa (banco de teste; nunca prod): verde.

## Resultado do enxugamento (sessão 2026-06-16)

Repo: **81,5 MB → 10,4 MB**. Material histórico/não-usado em `../auditoria-arquivo/`
(fora do repo) + recuperável no git. Mantido no repo: documentação canônica
(`docs/01–13` + guia + manuais), dados/POPs lidos em runtime, planilhas de pesos,
scripts operacionais e fixtures realmente usadas.
