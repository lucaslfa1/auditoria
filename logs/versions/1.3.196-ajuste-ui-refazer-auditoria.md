# v1.3.196 — Ajuste de UI: "Refazer Auditoria" + remoção do atalho de inverter locutor

**Data:** 2026-06-22
**Tipo:** Ajuste de UI (refino da v1.3.195)
**Branch:** `main`

## Contexto

Feedback do Lucas sobre a v1.3.195:
- O atalho **"Inverter operador/cliente"** é exagerado — o seletor de locutor por
  fala já resolve. Removido.
- O botão **"Reavaliar"** deve se chamar **"Refazer Auditoria"**.

## Mudança

- Removidos o botão "Inverter operador/cliente", o handler `handleInvertSpeakers`
  e o helper `swapOperatorClient` (`speakerLabels.ts`) — não usados mais.
- Botão renomeado para **"Refazer Auditoria"** (estado de carregamento:
  "Refazendo..."); texto de ajuda e toasts alinhados ("Auditoria refeita").
- Vale para as duas telas (`SavedFiles.tsx` e `AuditModal.tsx`).

O seletor de locutor por fala, o seletor de tipo de alerta e o fluxo de reavaliação
por IA continuam iguais.

## Validação

- `tsc -b` limpo; `eslint` 0 erros (4 warnings `any` pré-existentes); sem
  referências órfãs a `swapOperatorClient`/`handleInvertSpeakers`.
- `tests/frontend/frontend-regressions.test.mjs` verde (asserções atualizadas).

## Pendência

- Requer **deploy** (automático no push da `main`).
