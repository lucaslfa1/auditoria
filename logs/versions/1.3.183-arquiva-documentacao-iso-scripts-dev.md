# v1.3.183 — Arquiva documentação ISO antiga + scripts dev/one-off restantes

**Data:** 2026-06-16
**Tipo:** Limpeza de repositório (chore)
**Custo:** nenhum
**Branch:** `main`

## O que foi feito

Fechamento do enxugamento para handover: tudo que não é necessário foi movido para
`../auditoria-arquivo/` (fora do repo) e removido do versionamento. Recuperável
também pelo histórico git.

- **`documentacao/`** (32 arquivos): documentação ISO antiga (manual do usuário,
  guias de implantação, 21 prints de UI, PDF, relatório estrutural) — set paralelo
  e superado pela suíte canônica `docs/01–13`. Estava deletada no working tree
  (pendência anterior); recuperei do git p/ arquivar e oficializei a remoção.
- **88 scripts dev/one-off** de `scripts/` e `backend/scripts/`: debug/diagnose,
  probes de OBS/Huawei, testes manuais (não-pytest), `check_*` de inspeção,
  geradores de relatório/PDF one-off, `create_denise`/`find_denise`, `experiments/`,
  `patch_*`, `clear_*`, `set_hours`, etc. Verificados sem referência em
  `package.json`/docs/**tests**.

  > Correção: a varredura inicial pegou 90, mas 2 (`backend/scripts/huawei_unregistered_dry_run.py`
  > e `backend/scripts/probe_huawei_obs.py`) têm test files dedicados na suíte —
  > foram **restaurados** (são exercitados por testes). `scripts/utils/patch.py` é
  > falso positivo de grep (stem "patch" = `unittest.mock.patch`) e segue arquivado.

## Mantido no repo (operacional / regeneração / migração)

61 scripts: entry points (`start/stop/status-local`, `deploy-cloudrun`,
`new-log-version`), esteira Huawei (`run_huawei_sync`, `huawei_worker`, …),
`migration/` + `maintenance/`, `validate_*`/`sync_*`/`backfill_*`/`import_*`,
`generate_scoring_rules`/`generate_pesos_gestores`/`extract_pesos_from_xlsm`/
`regen_rag_criterios` (regeneração de dados), `db_*`/`manage_user_account`/
`list_tables`/`reconcile`/`reprocess`/`production_watchdog`, e
`db_knowledge_agent`/`sync_procedimento_chunks`.

## Validação

- Scripts arquivados: sem referência em package.json/docs (checagem automatizada).
- Suite backend completa (banco de teste; nunca prod): verde.

## Resultado FINAL do enxugamento (2026-06-16)

Repo: **81,5 MB → 9,9 MB** (1217 → 783 arquivos). O repo agora contém só
documentação canônica + arquivos efetivamente usados + tooling operacional.
Material histórico/dev em `../auditoria-arquivo/` (fora do repo) + git.
