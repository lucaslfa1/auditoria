# v1.3.151 — Extrai helpers de normalização de operators.py

**Data:** 2026-06-15
**Tipo:** Refatoração conservadora (sem mudança de comportamento)
**Custo:** nenhum
**Branch:** `main`

`repositories/operators.py` (~1459 linhas) mistura acesso a dados (CRUD de
colaboradores) com um cluster de helpers **puros** de normalização/parsing. Uma
análise (workflow, 6 candidatos avaliados por superfície de monkeypatch × valor)
apontou este cluster como a extração de melhor equilíbrio segurança/valor.

## O que mudou

- **NOVO** `repositories/operator_normalization.py`: 14 funções determinísticas
  (sem DB/rede/IA/env/estado de módulo):
  `_normalize_lookup_text`, `_map_status_telefonia_to_status`, `_is_active_status`,
  `_default_auditavel_from_status`, `_is_auditable_row`, `_coerce_auditavel`,
  `_normalize_operator_sector`, `_coerce_huawei_and_telefonia_ids`,
  `_resolve_huawei_id`, `_is_technical_telephony_row`, `_is_excluded_operation_row`,
  `_is_removed_operator_row`, `_pick_preferred_operator_id`,
  `_operator_payload_from_row`.
- `operators.py` reexporta os 14 nomes (compat total: callers e testes que usam
  `operators.<helper>` continuam resolvendo). Removido o `import unicodedata`,
  que só era usado pelo helper movido.

## Por que era seguro

- **Monkeypatch disjunto**: grep em `tests/backend` confirma que todo patch sobre
  `repositories.operators.*` mira apenas 5 funções de DB
  (`resolve_auditable_colaborador`, `buscar_colaborador_por_nome`,
  `buscar_colaborador_por_id_huawei`, `get_supervisores_e_escalas`,
  `listar_auditaveis_com_id_huawei`) — **zero** patches tocam os 14 helpers.
- O único teste que referencia um helper (`test_huawei_id_normalization.py` chama
  `operators._coerce_huawei_and_telefonia_ids`) é **chamada direta**, não patch —
  o reexport resolve.
- **Cluster fechado**: cada função só chama (a) siblings do próprio cluster ou
  (b) 3 deps estáveis (`normalize_huawei_agent_id` de `repositories.common`,
  `is_technical_telephony_values`/`is_excluded_operation_values` de
  `core.operator_filters`). Nenhuma dessas importa `operators` de volta → sem ciclo.
- O cluster sector-alias (`_get_sector_aliases_repo`, `_load_sector_aliases_dict`,
  `_map_organizacao_telefonia_to_sector`), que **fica**, estava interleaved com os
  helpers; a extração foi por nome/AST, não por range cego.

## Prova de equivalência

- `ast.dump` de cada uma das 14 funções: HEAD (`operators.py`) == módulo novo —
  **14/14 idênticas**, zero divergência.
- Reexport por identidade de objeto: `operators.X is operator_normalization.X`
  para os 14 nomes.
- `import repositories.operators` OK; `operators._coerce_huawei_and_telefonia_ids`
  resolve; `unicodedata` removido do namespace de `operators`.

## Validação

- Suite backend completa (banco de teste local; nunca prod): verde, sem regressão.

## Resultado

`operators.py`: 1459 → 1309 linhas; lógica de normalização isolada do acesso a
dados num módulo irmão coeso. Mesmo padrão validado em v1.3.141..148.
