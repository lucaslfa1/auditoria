# v1.3.206 — Exportacao do manual em PDF (script reutilizavel)

**Data:** 2026-06-24
**Tipo:** Documentacao / tooling
**Branch:** `main`

## Contexto

Necessidade de uma versao em PDF do manual consolidado (handover e leitura
offline), sem depender de pandoc/LaTeX (nao instalados).

## Mudanca

- Novo `scripts/build_manual_pdf.py`: converte o Markdown do manual em HTML
  estilizado (sumario automatico via extensao `toc`, codigo colorido via
  `pygments`, CSS de impressao A4 com capa) e usa o **Edge headless**
  (`--print-to-pdf`) para gerar o PDF. Pre-requisito: `pip install markdown`.
  Reutilizavel: rodar de novo regenera o PDF (util depois de colar os prints).
- Gerado `docs/manual-sistema/MANUAL-SISTEMA-AUDITORIA.pdf` (29 paginas: capa +
  sumario + corpo).
- `.gitignore`: passa a ignorar `docs/manual-sistema/_build/` (HTML intermediario
  + perfil do navegador).

## Arquivos

- `scripts/build_manual_pdf.py` (novo)
- `docs/manual-sistema/MANUAL-SISTEMA-AUDITORIA.pdf` (novo)
- `.gitignore`
- `logs/versions/1.3.206-exportacao-manual-pdf.md` (este)

## Validacao

- PDF conferido: header `%PDF-1.4`, 29 paginas, termina em `%%EOF`, ~744 KB.
- O unico diagrama mermaid (fluxo §3.2) sai como texto pre-formatado no PDF
  estatico; o mesmo fluxo tambem esta descrito em prosa (§3.1 e §6), entao nao
  ha perda de informacao.

## Pendencia

- Inserir os prints reais das telas em `docs/manual-sistema/imagens/` e
  regenerar o PDF com o script.
