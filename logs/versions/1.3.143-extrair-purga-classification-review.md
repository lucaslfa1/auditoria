# v1.3.143 — Extrai purga/descarte da fila de classification_review.py

**Data:** 2026-06-12
**Tipo:** Refatoração conservadora (sem mudança de comportamento)
**Custo:** nenhum
**Branch:** `main`

Continuação do split de `classification_review.py` (após o benchmark na v1.3.141).
Extrai o grupo **purga e descarte de itens da fila** para módulo próprio.

## O que mudou

- **NOVO** `backend/repositories/classification_review_purge.py`: 4 funções
  (`_unlink_item_media`, `_purgar_item_fila`,
  `limpar_fila_revisao_classificacao_antiga`, `descartar_item_automacao`) — a
  esteira binária v1.3.103 (tombstone permanent/recoverable + remoção de mídia).
- Grupo autocontido: depende só de `repositories.common.json_loads`. Os imports
  de `db.database.huawei_sync_log_tombstone` e
  `storage.audit_storage.resolve_stored_audit_audio_path` são lazy (dentro das
  funções) e viajaram junto — preserva o monkeypatch validado por
  `test_automation_tombstone_sync` / `test_automation_discard_unknown`.
- `classification_review.py` reexporta as 4 (compat total: import direto e
  fachada `db.database` seguem válidos; `limpar_*`/`descartar_*` estão na
  fachada).

## Prova de equivalência

- AST de cada uma das 4 funções movidas: **idêntico** ao HEAD (`ast.dump` igual).
- Reexport por identidade de objeto (`cr.X is purge.X`).
- Fachada `db.database` expõe `limpar_*` e `descartar_*`.
- Removidas do módulo antigo (confirmado).

## Validação

- Suite backend completa verde (banco de teste local; nunca prod).

## Estado de classification_review.py

Restam ~900 linhas: helpers (`_normalize_metadata_value`,
`_parse_metadata_datetime`), transições de status (sincronizar/tentar_iniciar/
atualizar_status/corrigir) e consultas (listar/obter_*/listar_paths). Próximo
corte possível: as consultas (read-only), que exigem um módulo de helpers
compartilhados (`_normalize_metadata_value`).
