# v1.3.125 — Limpeza de código morto e lixo do workspace + fix do seed não versionado

**Data:** 2026-06-11
**Tipo:** Higiene de repositório (handover)
**Custo:** nenhum em runtime
**Branch:** `handover`

## Removidos do git (recuperáveis via histórico)

### Código morto
- `backend/core/pre_triage.py` — pré-triagem de direção por áudio (Whisper+GPT).
  Sem chamadores desde a v1.3.115 (direção via VDN); única referência era o
  próprio teste. Recomendação 5.3 do relatório de consumo ("elimina o risco
  de a função voltar a escalar").
- `tests/backend/test_pre_triage.py` — teste do módulo removido.
- Patch obsoleto de `core.pre_triage` removido de `test_huawei_sync.py`.

### Lixo versionado na raiz e em tests/
`check_db.py`, `check_space.py`, `cleanup.py`, `commit_msg.txt`,
`commit_msg_sentry.txt`, `logs.json`, arquivo acidental `x[0])`,
`tests/backend/fix_tests{,2..6}.py` (scripts pontuais de correção),
`tests/backend/e2e_results.txt`.

### Disco (untracked, fora do repo)
Logs locais (`vite*.log`, `backend/service-*.log`, `backend/*.log`,
`connection_debug.txt`, `benchmark_results.txt` etc.). Rascunhos `tmp_*.py`
untracked permanecem no disco (já ignorados; invisíveis no repo).

## Decisões explícitas de NÃO remoção
- **`transcription_providers/speech_sdk_transcriber.py` FICA** — ao contrário
  do diagnóstico inicial, está VIVO: é o last-resort da cadeia de fallback
  (`transcription.py:1150`, estratégia `sdk`).
- **hybrid_dual FICA como legado documentado** (decisão do Lucas em
  2026-06-11): descontinuado, atrás de `AZURE_TRANSCRIPTION_ALLOW_LEGACY_HYBRID_DUAL`,
  não recomendado em docs novos.
- `scripts/` e `backend/scripts/`: triagem profunda adiada — a documentação
  canônica os marca como diagnósticos pontuais, não usados em produção.

## 🔴 Fix crítico: seed do catálogo não estava versionado
O `.gitignore` tem `*.sql` global ("Database Dumps") e **silenciosamente
excluiu** `backend/db/seeds/audit_catalog_oficial.sql` do commit da v1.3.120
— um clone novo não teria o catálogo. Corrigido: exceção
`!backend/db/seeds/*.sql` no .gitignore + arquivo versionado nesta versão.

## Validação
- ✅ `compileall` limpo após remoções.
- ✅ Suite completa: **847 passed, 0 falhas** (1 falha transitória do patch
  obsoleto corrigida no próprio commit).
