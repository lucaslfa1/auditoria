# v1.3.127 — Refatoração conservadora: huawei_sync.py dividido em módulos

**Data:** 2026-06-11
**Tipo:** Refatoração estrutural (movimentação pura, zero mudança de comportamento)
**Custo:** deploy backend
**Branch:** `handover`

## O que mudou

`backend/core/huawei_sync.py` (**2516 → 1663 linhas**) deixou de concentrar
triagem + classificação + enfileiramento. Extraídos para o pacote existente
`backend/core/huawei/`:

| Módulo novo | Conteúdo | Linhas |
| --- | --- | --- |
| `huawei/sync_triagem.py` | `_resolve_triagem_setor`, `_triagem_fallback`, `_aplicar_triagem_setorial` | 119 |
| `huawei/sync_classification.py` | `_classificar_audio_huawei`, `_classificar_pdf_huawei`, `_classificar_pendentes_async` (Fase 2), `_marcar_classificacao_status`, `_aplicar_auto_classificacao` + helpers | 660 |
| `huawei/sync_enqueue.py` | `_enfileirar_audio`, `_enfileirar_pdf`, `_enfileirar_classificado` | 177 |

## Compatibilidade (por que nada quebra)

- `huawei_sync.py` REEXPORTA os 13 nomes movidos — callers
  (`automation_engine`, `routers/telefonia`, scripts) e os patches da suite
  (`patch("core.huawei_sync.X")`) continuam funcionando.
- Dentro dos módulos novos, nomes compartilhados/patcháveis resolvem em
  runtime via `from core import huawei_sync as hs` (lazy, dentro de função)
  — preserva a patchabilidade dos testes e evita import circular.
- Strings, logs e assinaturas intactos byte a byte.

## Validação

- Testes por bloco durante a extração (88→92 passed nos arquivos huawei).
- **Prova A/B**: suite completa ANTES (stash) e DEPOIS da refatoração no
  mesmo ambiente → números idênticos.
- Gate final: **836 passed, 47 skipped, 0 failed** (os 11 passed a menos vs
  v1.3.124 são os testes do `pre_triage` removido na v1.3.125 — invariante é
  o ZERO failed).

## Decisão registrada (limite da refatoração conservadora)

`routers/telefonia.py` (~2270) e `db/database.py` (~2200) **não serão
divididos** neste handover: o ganho de navegação será entregue por
comentários de seção + docstrings (v1.3.128), sem o risco de regressão de
mover centenas de linhas a dias da entrega. `classification.py` idem.
Recomendação ao time: dividir depois, com a suite verde como rede.
