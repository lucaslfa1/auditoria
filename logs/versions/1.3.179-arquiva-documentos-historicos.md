# v1.3.179 — Arquiva documentos/dados históricos para fora do repo

**Data:** 2026-06-16
**Tipo:** Limpeza de repositório (chore)
**Custo:** nenhum
**Branch:** `main`

## Contexto

Continuação do enxugamento para handover (após v1.3.178). Decisão do Lucas:
manter no repo só a documentação canônica + arquivos usados; mover o material de
valor **histórico mas não usado** para uma pasta FORA do repo
(`../auditoria-arquivo/`, espelhando a estrutura). Tudo segue recuperável também
pelo histórico git.

## Arquivado (165 arquivos, ~23.7 MB → `../auditoria-arquivo/`)

- `instrucoes/lista-de-funcionarios/*.xlsx` (snapshots de RH com **PII** — não
  devem ficar num repo de handover; eram semente de import já executado).
- PDFs/DOCX de referência da API Huawei (`docs/integracoes/huawei/*.pdf`, `*.docx`,
  `raw/`) — o conteúdo útil já está nos `.md` (catálogo) que **permanecem**.
- `relatorios_atividades/` (relatórios datados), `Ajustes IA/`, `telefonia/`
  (docx de trabalho/troca), `export/` (apresentações/propostas/relatórios one-off),
  `templates/Template_nstech.pdf`.
- `rag/sources/auditoria_principais_erros.pdf` + `rag/FECHAMENTO DAS AUDITORIAS.docx`
  (não lidos em runtime; os POPs `.md` permanecem).
- Docs legados: `docs/{architecture,arquitetura,database,reviews,reports,backlog,branding}`,
  `docs/database.md`, `docs/{GUIA_DE_SOBREVIVENCIA_AUTOMACAO,SYSTEM_DOCUMENTATION,MIT_BEST_PRACTICES_REPORT}.md`,
  `docs/relatorio-*.md`, `docs/Fluxo IA.xlsx`, `docs/references/legado-arquitetura-sqlite-testes.md`.
- `planejamento/*` (planejamento de fases passadas) — **exceto** os 2 com valor
  operacional vigente: `GUIA_EXECUCAO_LOCAL_HUAWEI.md` e `RELATORIO_IP_WHITELIST_HUAWEI.md`.

## Mantido no repo (USADO / DOC canônico)

Suíte `docs/01–13` + `README` + `manual-gestores/` + `references/auditoria/` +
`infra/` + `integracoes/huawei/*.md` + `postman_collection.json`;
`rag/sources/procedimentos_operacionais/*.md` + `rag/README|CHANGELOG`;
`instrucoes/workflow/*.xlsm` (pesos) + `Manual tecnico Qualidade.md`;
`docs/Lista - Operadores e Supervisores.xlsx` (lida por seed); fixtures usadas
por testes (inclui o `.wav` de 13.7 MB do teste de integração — mantido por
decisão do Lucas).

## Resultado

Repo: **81,5 MB → 24,1 MB** somando v1.3.178 + v1.3.179. Suite backend verde.
Próximo passo possível: revisar scripts one-off de `scripts/`/`backend/scripts/`
(refactor/diagnose/fix/benchmark) — pequenos em MB, a tratar num passo à parte.
