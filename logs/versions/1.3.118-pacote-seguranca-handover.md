# v1.3.118 — Pacote de segurança do handover (.env.example completo + docs/08-seguranca.md)

**Data:** 2026-06-11
**Tipo:** Segurança / documentação de handover
**Custo:** nenhum em runtime (sem mudança de código de produção)
**Branch:** `handover`

## Mudanças

### `backend/.env.example` reescrito (600 linhas, 179 variáveis)
- Inventário completo via grep de `os.getenv`/`os.environ` no backend.
- 17 seções (DB, Auth, Azure OpenAI/Speech/Whisper/Diarize, Transcrição,
  Guardrails de Custo, Huawei, Automação, Cron, Storage, Sentry...).
- Cada variável: comentário PT-BR (o que faz + default do código), marca
  `[CUSTO]` nas 31 que geram/controlam custo pago, marca `(env>DB)` nas que
  têm fallback na tabela `configuracoes`.
- ~140 variáveis que faltavam foram adicionadas (todas as `DB_*`, `COST_*`,
  knobs de transcrição/automação, ~20 Huawei, etc.).
- Removidas obsoletas (zero referência no código): `GEMINI_API_KEY` (provedor
  primário é Azure), `AI_PRIORITY` (código lê `AI_PROVIDER_PRIORITY`),
  `ENABLE_AZURE_TRANSCRIPTION`, `SQLSERVER_READONLY_*` (12 vars — integração
  é só plano em docs/database/), `ASSEMBLYAI_*`, `VITE_AI_API_KEY`.
- **Nenhum valor real copiado** (verificado por grep dos prefixos das chaves).

### `.env.example` (raiz) reescrito
`VITE_API_URL` (única var VITE_* lida no código) + fallbacks opcionais de dev
local comentados, com aviso do guard anti-prod dos testes.

### `docs/08-seguranca.md` (novo)
Inventário de 13 segredos; **incidente documentado**: `.env` com chaves reais
nos commits `81818b66`, `b8b7e1da`, `df3878f8` (verificados) → chaves
COMPROMETIDAS, rotação obrigatória (Lucas/CI/CD), decisão de NÃO reescrever
histórico; tabela env→Azure Key Vault com exemplo de `secretRef`; descrição
verificada de auth (HMAC, bcrypt, roles, rate limit de login), CORS fail-fast,
security headers, rate limit global, cron fail-closed; guardrails de custo
como segurança financeira; checklist de 12 itens do handover.

### Validações de código que NÃO precisaram de mudança
- `_is_production_environment()` (main.py:233) já decide por `ENVIRONMENT`
  explícito (não `K_SERVICE`).
- `media_storage._get_default_backend` já honra `ENVIRONMENT=production` além
  de `K_SERVICE` (linha 66). Usos restantes de `K_SERVICE` são benignos
  (Sentry tags, fallback de bucket) — tratados na etapa Azure (v1.3.127+).

## Achado lateral (sem ação nesta etapa)
Os `.env` reais contêm variáveis mortas (`AZURE_WHISPER_AUTH_MODE`,
`GCP_PROJECT_ID`, `GCP_LOCATION`, `VERTEX_DATA_STORE_ID`) — limpar quando
conveniente.

## Validação
- ✅ grep dos prefixos de chaves reais nos 3 arquivos novos: zero ocorrências.
- ✅ Sem mudança de código → sem rerun da suite (estado = v1.3.117).
