# v1.3.181 — Arquiva scripts one-off + remove dead code de core/

**Data:** 2026-06-16
**Tipo:** Limpeza de repositório (chore)
**Custo:** nenhum
**Branch:** `main`

## O que foi feito

Último passo do enxugamento de handover. Auditoria classificou os scripts de
`scripts/` e `backend/scripts/` em operacionais (mantidos) vs one-off já executados.

**Arquivados (63 scripts → `../auditoria-arquivo/`)** — one-off de alta confiança,
verificados SEM referência em `package.json`/docs/Dockerfile: `scratch_*`,
`refactor_*`, `fix_*` (correções ad-hoc já aplicadas), `diagnose_*`, `benchmark*`,
`gerar_relatorio_word*`/`gerar_revisao_word`, `rpa_download_ligacoes`, sondas de
bring-up (`test_aicc/dns/endpoints/last_hour/quality`), `align_weights`/`analyze_*`/
`compare_weights`, `check_*` utilitários de diagnóstico, `update_*` de config,
`sync_db_official`, e `backend/scripts/utils/_archived_root_scripts/`.

**Removidos (dead code em `core/`)**:
- `backend/core/fix_huawei_sync.py` (script one-off `fix_*`, não importado).
- `backend/core/huawei/protocols.py` (stub de 34 bytes, só `from typing import ...`,
  sem definições; não importado por ninguém).

**Mantidos (operacionais / regeneração / referenciados)**: `start/stop/status-local`,
`deploy-cloudrun`, `new-log-version`, esteira de sync Huawei (`run_huawei_sync`,
`huawei_worker`, etc.), `scripts/migration/*` (tooling de migração documentado em
docs/10), `backend/scripts/{db_knowledge_agent,sync_procedimento_chunks,validate_*,
sync_*,check_queue,release_lock,clear_sync_history,import_*,backfill_*,...}`,
`maintenance/`, e os ~104 scripts que não casaram padrão claro de one-off (mantidos
por conservadorismo — pequenos; um próximo passo pode revisá-los caso a caso).

## Validação

- Os 63 arquivados: sem referência em package.json/docs (checagem automatizada).
- Os 2 dead code: 0 referências (grep em backend/src/tests/scripts).
- Suite backend completa (banco de teste; nunca prod): verde.

## Resultado final do enxugamento (2026-06-16)

Repo: **81,5 MB → 10,1 MB** (v1.3.178–181). Material histórico/one-off em
`../auditoria-arquivo/` (fora do repo) + recuperável no git. Repo agora contém
documentação canônica + arquivos efetivamente usados.
