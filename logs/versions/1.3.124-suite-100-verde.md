# v1.3.124 — Suite de testes 100% verde (847 passed, 0 falhas)

**Data:** 2026-06-11
**Tipo:** Qualidade — saneamento completo da suite (handover)
**Custo:** deploy backend (1 fix de código no caminho legado)
**Branch:** `handover`

## Resultado

`pytest tests/backend -q` contra banco de teste limpo:
**847 passed, 47 skipped, 0 failed** — eram **28 falhas** no baseline
(v1.3.113). Critério de gate de todas as etapas seguintes do handover.

## Causas raiz das 28 falhas (diagnóstico)

| Grupo | Qtde | Causa | Correção |
|---|---|---|---|
| Guardrails de classificação | 13 | Catálogo incompleto em banco novo | Seed oficial (v1.3.120) |
| Label de setor hardcoded | 1 | Labels editáveis desde v1.3.106 | Assert dinâmico vs catálogo |
| Gates de transcrição (risco/cache) | 4 | Fixtures sem `transcription_provider.selected_strategy` (exigido pela política do candidate selector) | Fixtures atualizados |
| Contrato da fila de revisão | 6 | Idem (4) + default `AUTOMATION_TRANSIENT_RETRY_LIMIT=1` da v1.3.111 (2) | Fixtures + env fixada p/ testar roteamento de retry |
| Orquestrador de transcrição | 2 | Asserts pré-v1.3.109 (hybrid_dual com fallback; melhor-candidato sem flag) | Asserts no contrato atual + teste novo do modo estrito |
| hybrid_dual robustez | 1 | **BUG REAL**: branch de falha da fusão não respeitava `allow_degraded_hybrid_fallback` (Diarize/Whisper respeitavam) | **Fix em `core/transcription.py`** |
| Selector escalando p/ premium | 1 | Teste codificava comportamento pré-corte de custo | Reescrito: escala p/ gpt4o SÓ quando fast/whisper não prestam |
| Recuperação de áudio / e2e | 2 | Fixtures sem selected_strategy | Fixtures atualizados |

## Fix de código (único)

`backend/core/transcription.py` — no engine LEGADO hybrid_dual
(**descontinuado**, só roda com `AZURE_TRANSCRIPTION_ALLOW_LEGACY_HYBRID_DUAL`;
decisão do Lucas: manter como legado documentado, não remover): falha da
fusão GPT-4o agora respeita o fallback degradado para Fast no fluxo manual
(`allow_degraded_hybrid_fallback=True`), igual aos branches de falha do
Diarize/Whisper. Modo estrito (automação) continua bloqueando.

## Princípio aplicado

Cada teste foi classificado como (a) desatualizado vs mudança INTENCIONAL —
atualizado citando a versão que mudou o contrato — ou (b) bug real —
corrigido o código. Nenhum assert foi enfraquecido sem justificativa
documentada no docstring.

## Observação

`tests/backend` agora é confiável como gate de regressão para o time de
engenharia: banco de teste local (`auditoria_test`), guard contra produção
no conftest, zero falhas toleradas.
