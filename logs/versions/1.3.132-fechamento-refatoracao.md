# v1.3.132 — Fechamento da refatoração: docstrings 100% + workflow Node 24 + higiene

**Data:** 2026-06-12
**Tipo:** Documentação de código + manutenção de CI + limpeza
**Custo:** nenhum em runtime
**Branch:** `main`

## Contexto

Decisão de escopo: o setor do Lucas É o time de engenharia — nada fica como
"recomendação a terceiros". Esta versão executa os itens que estavam
pendentes como handoff e fecha a cobertura de documentação do backend.

## 1. Workflow de deploy pronto para Node 24 (urgente)

O GitHub força Node 24 nos runners a partir de **16/06/2026**; as actions
usadas rodavam em Node 20 (aviso em toda run). Atualizadas em
`deploy-cloudrun.yml`:

| Action | Antes | Depois |
| --- | --- | --- |
| `actions/checkout` | v4 | v6 |
| `google-github-actions/auth` | v2 | v3 |
| `docker/login-action` | v3 | v4 |
| `google-github-actions/deploy-cloudrun` | v2 | v3 |

Bônus estrutural: o deploy agora injeta `GIT_COMMIT_SHA=${{ github.sha }}`
como env do serviço a cada publicação — o commit exibido em `/api/health`
não fica mais defasado entre deploys (causa raiz do "1.3.108" fantasma).

## 2. Docstrings PT-BR — backend 100% coberto

Conclui a frente iniciada na v1.3.129. Arquivos desta leva (~1.300 linhas
de documentação; trabalho parcial de agentes + finalização manual):

| Arquivo | Cobertura |
| --- | --- |
| `core/transcription.py` | módulo + todas as funções (cadeia de engines, selector, judge, custos por chamada) |
| `core/audit_evaluator.py` | módulo + todas as funções (avaliação GPT-4o, retry, cost_guard) |
| `core/audit_pipeline.py` | módulo + todas as funções |
| `routers/telefonia.py` | cabeçalho com mapa de endpoints/custo + docstring em TODOS os handlers e helpers |
| `db/database.py` | docstring de módulo (papel de fachada, decisão v1.3.127 de não dividir) + 86 funções (delegações com docstring padrão gerada e auditada; lógica real documentada à mão) |
| `repositories/audits.py` | completo |
| `repositories/classification_review.py` | completo (fila de triagem, statuses, regras de cota mensal) |

**Critério de verificação programática:** script AST confirma 0 funções/
classes sem docstring nos 7 arquivos; prova de equivalência funcional por
comparação de AST (sem docstrings/strings inertes) contra o HEAD — única
diferença real é o item 3 abaixo, intencional.

## 3. Limpeza

- `db/database.py`: removido bloco DUPLICADO no fim do arquivo
  (`list_pending_dispatch_audits`, `upsert_audit_draft`, `get_audit_draft`
  definidos 2× idênticos — a 2ª definição sobrescrevia a 1ª; zero impacto).
- `scripts/utils/old_sync.py` e `old_client.py` removidos (~100 KB de cópias
  pré-refactor v1.3.127; histórico permanece no git).
- `package.json` `version`: 1.3.92 → 1.3.132 (fallback de versão do
  `/api/health` coerente).

## Validação

- ✅ Verificador AST: 0 docstrings faltantes nos 7 arquivos; diffs
  funcionalmente equivalentes ao HEAD (exceto remoção da duplicata)
- ✅ `py_compile` limpo em todos os arquivos tocados
- ✅ Suite backend completa no banco de teste local
- ✅ Deploy do workflow atualizado validado em produção no push desta versão

## Limite de sessão (registro operacional)

6 dos 7 agentes de documentação morreram por limite de sessão (reset
12:50). O trabalho parcial deixado por eles compilava e era AST-equivalente;
foi auditado e completado manualmente no loop principal.
