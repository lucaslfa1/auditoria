# v1.3.198 — Documentação no código: "como cada processo é disparado/implantado"

**Data:** 2026-06-22
**Tipo:** Documentação (comentários no código — risco zero, sem mudança de comportamento)
**Branch:** `main`

## Contexto

Lucas pediu para verificar as partes do sistema onde o código NÃO está comentado
explicando como cada processo é implantado/disparado, e preencher as lacunas (em
etapas). Levantamento (3 varreduras read-only) apontou: o miolo já é bem
comentado e há a pasta `docs/` (01–13) cobrindo deploy/operação; as lacunas
reais eram pontuais — gatilho do cron, scripts sem cabeçalho, fases do sync sem
"quem dispara" e várias telas do frontend sem cabeçalho.

## Mudança (3 etapas, só comentários/docstrings)

**Etapa 1 — deploy/cron/scripts:**
- `routers/automation.py` (`/cron/run`): documenta o SETUP do agendador (URL,
  `Authorization: Bearer <CRON_SECRET_TOKEN>`, origem `cloud_scheduler`, refs docs).
- Cabeçalhos com "COMO RODAR" em `scripts/db_migrate.py`,
  `scripts/create_db_migration.py`, e bloco de help em `scripts/deploy-cloudrun.ps1`
  (notando que o deploy canônico é o GitHub Actions).
- `scripts/huawei_worker.py` e `scripts/huawei_manual_sync.py`: deixam claro que
  são ferramentas LOCAIS (não-deploy); em produção a coleta roda pelo cron.

**Etapa 2 — gatilhos dos processos backend:** linha "DISPARO:" em
`core/huawei/sync_triagem.py`, `sync_enqueue.py`, `sync_classification.py`,
`download_candidates.py` (Fases 1/2 do `executar_sync_huawei`), `core/rag_triagem.py`
(BackgroundTasks após salvar correção) e `core/quality_analyzer.py` (advisory,
pré-transcrição).

**Etapa 3 — telas do frontend:** cabeçalho (propósito + fluxo + endpoints) em
`audit/hooks/useAuditFlow.ts`, `useAuditOrchestrator.ts`, `AuditWorkspace.tsx`,
`classifier/hooks/useClassifier.ts`, `admin/*` (criteria, ai-prompts,
sector-aliases, SectorManagement), `ai-feedback/*` (page + modal),
`fechamento/FechamentoPage.tsx`, `review/ReviewPage.tsx`,
`telefonia/TelefoniaPage.tsx`, `automacao/AutomacaoPage.tsx`,
`settings/Settings.tsx`, `colaboradores/ColaboradoresPage.tsx`.

## Validação

- `python -m py_compile` nos 11 arquivos Python alterados: OK.
- `tsc -b` limpo; `tests/frontend/frontend-regressions.test.mjs` verde.
- `eslint`: nenhum problema NOVO — os erros/warnings nesses arquivos são
  pré-existentes (provado via `git stash`: ex. `AuditWorkspace` set-state-in-effect
  na linha 178 antes / 187 depois, mesmo erro só deslocado pelo comentário). Fora
  do escopo (pedido foi documentar, não corrigir lógica).

## Pendência

- Sub-painéis de `settings/` (AutomationSettings, ThemeSettings, etc.) e alguns
  utilitários de `core/` (json_repair, sentiment, network_utils...) ficaram sem
  cabeçalho — baixo valor; documentar sob demanda.
- Requer **deploy** (automático no push da `main`) — mas é só comentário.
